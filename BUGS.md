# BUGS & AMENDMENTS

## Infocare Analytics

[x] When clicked on a service in the list the list jumps to the top when it should stay visible to the user - on overflow visible section
[x] The "Refresh" button behaviour currently pulls data from Infocare API and will require these changes:

- Move the button and function to the Infocare Analytics title secion next to the `Open window` button
- Change the icon to a download symbol
- Replace the "Refresh" button with a differnt behaviour to reset the Infocare Analytics panel to default removing the up/down arrow setting and going back to the `3M` filter default

## AI Chat

[x] The height of the title bar should be the same height as all title bars - ake it minimum height of 36px
